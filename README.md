# Teste prático para converter números romanos em arábicos e criação do treemap

## Para rodar o projeto, clone este repositório, após, rodar os comandos abaixo:

```bash
npm install

npm run dev
```

